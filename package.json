{
  "scripts": {
    "play:win": "start /b npm run dev --prefix server && npm run dev --prefix client",
    "play:mac": "sh -c '(cd server && npm run dev) & sleep 3 && (cd client && npm run dev) & wait'"
  }
}
