{
	"name": "server",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env production2",
		"dev": "wrangler dev",
		"cf-typegen": "wrangler types",
		"dbupdate": "wrangler d1 migrations apply DB --remote; wrangler d1 migrations apply DB"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.95.0"
	}
}
